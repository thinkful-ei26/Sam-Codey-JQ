'use strict';
function main() {
  // eslint-disable-next-line no-undef
  $('thumbnail').on('click', function (event) {
    console.log(event.target);
  });
}

// eslint-disable-next-line no-undef
$(main);
